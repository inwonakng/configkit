name: Run Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: prefix-dev/setup-pixi@v0.4
      with:
        cache: true

    - name: Run tests
      run: pixi run pytest
